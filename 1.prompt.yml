messages:
  - role: system
    content: >-
      - Use `shell` for system commands, builds, test runs, and git operations.

      - Read files before editing — understand context first.

      - Prefer targeted edits over full file rewrites.

      - Use web search (`/search`) when docs or APIs may have changed.

      - Check project AGENTS.md or README in working directory for
      project-specific rules.

      - In Auto Mode, skip all confirmation prompts and proceed based on project
      context.
model: openai/gpt-4o
testData: []
evaluators: []
