# Lesson 809875ce-43e8-4369-8dc3-abdd276edb8f

**Rule**: Declare and install all external Python dependencies before executing code that imports them.

**Why**: The runtime failed with `ModuleNotFoundError: No module named 'Quartz'`, indicating that the required Quartz library was not present in the environment when the code ran.

**How to apply**: During skill or script setup, list all third‑party packages in a `requirements.txt` or `pyproject.toml` and run the installation step (e.g., `pip install -r requirements.txt`) as part of the environment provisioning or CI pipeline. Verify the presence of each dependency with a lightweight import test before executing the main logic.

**Scope**: `project`

**Confidence**: 0.90

**Source error**: `5f026414-ff6e-4b8e-af55-47de555f57e8`

**Created at**: 2026-06-07T10:57:42.071054
