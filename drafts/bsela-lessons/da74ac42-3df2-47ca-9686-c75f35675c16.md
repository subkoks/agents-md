# Lesson da74ac42-3df2-47ca-9686-c75f35675c16

**Rule**: Before committing a new CLI command, run the command with the `--help` flag and programmatically compare the displayed options to the actual Typer signatures to catch any mismatches.

**Why**: Multiple errors reported an incorrect or incomplete help text (`Usage: ao [options] [command]…` and `name: BSELA phase state snapshot…`) indicating that the auto‑generated help did not reflect the actual command definitions, leading to user confusion and build failures.

**How to apply**: Add a pre‑commit hook or CI step that executes `python -m bsela.cli --help` (or the relevant entry point) and parses the output, asserting that every flag and positional argument listed matches the Typer `Option`/`Argument` decorators in source code. Flag any discrepancies before allowing the commit or pipeline to proceed.

**Scope**: `project`

**Confidence**: 0.75

**Source error**: `c03734a1-7861-4d37-babe-3f0d21220376`

**Created at**: 2026-04-28T20:07:24.805747
